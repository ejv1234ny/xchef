# xchef (working name)

Phone-first restaurant usage & inventory app beside Toast POS. Start with `CLAUDE.md`, then `docs/KICKOFF.md` for the Claude Code prompts. Data layer: `docs/schema.sql` (applied as `supabase/migrations/0001_init.sql`). Full design: `docs/architecture.md`.

Supabase project: `xchef-dev` (ref `gqahyzoebifscqcrrkgq`, us-east-1). Migrations 0001 and 0002 are already applied there.
