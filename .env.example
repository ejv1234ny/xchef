NEXT_PUBLIC_SUPABASE_URL=https://gqahyzoebifscqcrrkgq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
ANTHROPIC_API_KEY=
ANTHROPIC_WORKSPACE_ID=           # only for identity-linked keys (console → Settings → Workspaces, wrkspc_…)
CRON_SECRET=
# Inbound email (Resend). Webhook: https://xchef.vercel.app/api/inbound/resend, event email.received
RESEND_API_KEY=            # Resend → API Keys (full access; needed to fetch received attachments)
RESEND_WEBHOOK_SECRET=     # Resend → Webhooks → the endpoint's signing secret (whsec_…)
INBOUND_EMAIL_ADDRESS=     # the address vendors/you forward invoices to, e.g. invoices@<id>.resend.app
POSTMARK_INBOUND_SECRET=   # deprecated — Postmark route stays until Resend has processed 10 real invoices
TOAST_API_HOST=https://ws-api.toasttab.com
